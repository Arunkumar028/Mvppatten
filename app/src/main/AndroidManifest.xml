<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mvp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".roomdatabse.UpdateTaskActivity"
           android:icon="@drawable/ic_launcher_background"

            />
        <activity android:name=".roomdatabse.RoomDatabaseActivity" />
        <activity android:name=".CalendarTask.CalendarActivity"
            android:icon="@drawable/ic_launcher_background">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".view.activity.MvppattenActivity" />
        <activity android:name=".roomdatabse.AddTaskActivity" />
        <activity android:name=".view.activity.MainActivity">

        </activity>
    </application>

</manifest>